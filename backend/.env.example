PORT=5000
NODE_ENV=development
JWT_SECRET=supersecretvendorbridgekey12345
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=yourpassword
DB_NAME=vendorbridge
# If set to true, forces fallback to the simulated repository
MOCK_DB=false
